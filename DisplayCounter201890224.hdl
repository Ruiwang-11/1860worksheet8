CHIP DisplayCounter201890224{
    IN inc, reset;
    OUT a,b,c,d,e,f,g;
    PARTS:


    Counter(inc=inc, reset=reset, out=bits);
    Decoder201890224(
    A=bits[0],
    B=bits[1],
    C=bits[2],
    D=bits[3],
    a=a, b=b, c=c, d=d, e=e, f=f, g=g
);
}