CHIP DisplayCounter201890224{
    IN inc, reset;
    OUT a,b,c,d,e,f,g;
    PARTS:


    Counter(inc=inc, reset=reset, out=temp);
    Decoder201890224(
    A=out0,
    B=out1,
    C=out2,
    D=out3,
    a=a, b=b, c=c, d=d, e=e, f=f, g=g
);
}