CHIP Counter{
    IN inc, reset,
    OUT out0, out1, out2, out3;
    PARTS:
    Inc16(in=registerout,out=intomux);
    Mux16(a=intomux,b=outmux2,sel=inc,out=outmux);
    Mux16(a=intomux,b=false,sel=reset, out=outmux2);


    Register(in=outmux2,load=true,out=registerout，out=out);
}